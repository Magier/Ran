name: List Kubernetes secrets
description: Get a Kubernetes secrets via the API server
tactic: Credential Access
techniques: ["Unsecured Credentials: Container API", "T1552.007"]
ttp: T1552.007
preconditions:
  can-get: ["Secrets"]
parameters:
  TOKEN:
    type: string
    description: the ServiceAccount token used to authorize this request
    optional: true
    default: ""
  NS:
    type: string
    description: specify the namespace to get pods from
    default: ${NS}
  ALL_NS:
    type: bool
    description: get pods from all namespaces or just the current one
    default: false
cmdVariants:
  - key: kubectl
    command: >-
      kubectl get secrets --token=${TOKEN} -n=${NS} -A=${ALL_NS} --output=json
  - key: curl
    command: >-
      curl -XGET 
      -H "Accept: application/json;as=Table;v=v1;g=meta.k8s.io,application/json;as=Table;v=v1beta1;g=meta.k8s.io,application/json"
      -H "User-Agent: kubectl/v1.30.3 (linux/amd64) kubernetes/6fc0a69"
      -H "Authorization: Bearer ${TOKEN}"
      '${API_SERVER}/api/v1/namespaces/${NS}/secrets?limit=500
effects:
  - k8s.secretList
references:
  - https://microsoft.github.io/Threat-Matrix-for-Kubernetes/techniques/List%20K8S%20secrets/
  - https://attack.mitre.org/techniques/T1552/007/
