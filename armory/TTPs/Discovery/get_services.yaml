id: get-services
name: Get Services via API Server
description: Enumerate Kubernetes Services via the K8s API server to map
  internal service endpoints and identify externally exposed workloads
tactic: Discovery
techniques: [ "Container and Resource Discovery", T1613 ]
preconditions:
  rbac:
    - verb: list
      resource: services
parameters:
  K8S_AUTH:
    type: K8sAuth
    description: the Kubernetes identity selected by Authenticate As
  NS:
    type: Namespace
    description: namespace to enumerate
    default: ${NS}
  ALL_NS:
    type: bool
    description: enumerate services across all namespaces
    default: false
procedures:
  - key: kubectl
    command: >-
      kubectl get services ${K8S_AUTH} -n=${NS} -A=${ALL_NS} --output=json
  - key: k8s-request
    k8s_request:
      authentication: ${K8S_AUTH}
      api_server: ${API_SERVER}
      api: /api/v1
      resource: services
      namespace: ${NS}
      cluster_scoped: ${ALL_NS}
      query: limit=500
      use_ca: false
effects:
  - k8s.servicelist
references:
  - https://kubernetes.io/docs/concepts/services-networking/service/
  - https://attack.mitre.org/techniques/T1613/
